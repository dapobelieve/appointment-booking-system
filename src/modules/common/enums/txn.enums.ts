export enum TransactionEnums {
  DELIVERY = 'DELIVERY',
  RECONCILIATION = 'RECONCILIATION',
}

export enum TransactionStatus {
  PENDING = 'pending',
  SUCCESS = 'success',
  FAILED = 'failed',
  REJECTED = 'rejected',
}
